{% cmp 'input',
    classBase='form-range',
    class=attr.class,
    style=attr.style,
    id=attr.id,
    name=attr.name,
    disabled=attr.disabled,
    readonly=attr.readonly,
    sizing=attr.sizing,
    value=attr.value,
    min=attr.min,
    max=attr.max,
    step=attr.step,
    error=attr.error,
    success=attr.success,
    type='range' %}{% endcmp %}