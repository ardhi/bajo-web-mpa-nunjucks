<{{ attr.tag|d('div') }}
    class="card-footer{{ attr.class }}"
    style="{{ attr.style }}"
    {{ attributes|safe }}
    {{ params|join(' ') }}>
    {{ attr.content|d(content|safe) }}
    </{{ attr.tag|d('div') }}>